---
title: IMQA Web Agent 설치
description: IMQA OpenTelemetry 자동 계측 JavaScript 라이브러리는 브라우저에서 추적을 활성화합니다.
nav: 2
---

## 설치

웹사이트에서 IMQA로 계측을 시작하는 가장 쉬운 방법은 아래 코드를 `<head></head>` 태그 내부에 추가하는 방법입니다:

```html
<script src="./js/web-agent-v0.0.1.min.js" type="text/javascript"></script>
<script>
    window.IMQA.init({
      collectorUrl: '{{IMQA_TRACES_COLLECTOR_URL}}',
      serviceName: '{{NAME_OF_YOUR_WEB_SERVICE}}',
      serviceVersion: '{{VERSION_OF_YOUR_WEB_SERVICE}}',
      serviceKey: '{{KEY_OF_YOUR_WEB_SERVICE}}'
    });
</script>
```

IMQA 지원하는 모든 옵션은 [설정](#설정)을 참조해주세요. `collectorUrl` 및 `serviceName`을 올바르게 제공하면 웹사이트가 준비되며 다른 필수 작업은 필요하지 않습니다. 수집된 추적 정보를 지정된 IMQA 수집기로 전송합니다.

```html
<script>
  (function (w, s, d, r, e, n) {
    (w[s] = w[s] || {
      readyListeners: [],
      onReady: function (e) {
        w[s].readyListeners.push(e);
      },
    }),
      ((e = d.createElement('script')).async = 1),
      (e.src = r),
      (n = d.getElementsByTagName('script')[0]).parentNode.insertBefore(e, n);
  })(window, 'IMQA', document, './js/web-agent-v0.0.1.min.js');

  window.imqa.onReady(function () {
    window.IMQA.init({
      collectorUrl: '{{IMQA_TRACES_COLLECTOR_URL}}',
      serviceName: '{{NAME_OF_YOUR_WEB_SERVICE}}',
      serviceVersion: '{{VERSION_OF_YOUR_WEB_SERVICE}}',
      serviceKey: '{{KEY_OF_YOUR_WEB_SERVICE}}'
    });
  });
</script>
```
> [!NOTE]
> **현재 미지원** 자동 업데이트를 위해 CDN을 사용할 수 있습니다. https://cdn.imqa.io/web-agent.min.js

> [!NOTE]
> 위 예제는 `src` 속성의 스크립트 버전을 생략하고 가장 최신 버전을 자동으로 사용합니다. 스크립트의 버전을 수동으로 제어하려면:
> - `web-agent-vX.min.js` (예: `web-agent-v1.min.js`)을 사용하여 주요 버전을 제어 (호환되지 않는 변경 없음),
> - `web-agent-vX.Y.js` (예: `web-agent-v1.0.min.js`)으로 주요 버전 제어 (버그 수정만 자동 포함),
> - `web-agent-vX.Y.Z.js` (예: `web-agent-v1.0.0.min.js`)로 주요 버전 제어 (엄격한 버전 제어).

## 수동 설치 (현재 지원되지 않음)

다른 옵션은 프로젝트 내에서 이 라이브러리를 번들로 묶고 초기화하는 것입니다. 프로젝트 디렉토리 내에서 `npm install @imqa/web-agent`를 실행하세요.

에이전트는 다른 기능보다 우선적으로 초기화되어야 관련 정보들을 수집할 수 있습니다.

```javascript
import IMQA from '@imqa/browser';

IMQA.init({
  collectorUrl: '{{IMQA_TRACES_COLLECTOR_URL}}',
  serviceName: '{{NAME_OF_YOUR_WEB_SERVICE}}',
  serviceVersion: '{{VERSION_OF_YOUR_WEB_SERVICE}}',
  serviceKey: '{{KEY_OF_YOUR_WEB_SERVICE}}'
});
```

## 옵션 목록

| 옵션 이름                    | 타입                                            | 필수 여부 | 설명                                                                                                                                                                 |
|-----------------------------|-------------------------------------------------|-----------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `collectorUrl`              | `string`                                        | 선택      | 콜렉터 주소를 지정합니다. 기본값은 `https://in-otel.imqa.io`입니다.                                                                                                |
| `authorizationToken`        | `string`                                        | 선택      | 인증 토큰을 설정합니다.                                                                                                                                               |
| `serviceName`               | `string`                                        | 필수      | 서비스 이름을 지정합니다. 텔레메트리 데이터 저장에 필수적입니다.                                                                                                      |
| `serviceKey`                | `string`                                        | 필수      | 서비스 키를 설정합니다. 텔레메트리 데이터 저장에 필수적입니다.                                                                                                        |
| `serviceVersion`            | `string`                                        | 필수      | 서비스 버전을 지정합니다. 텔레메트리 데이터 저장에 필수적이며, 빈 문자열이나 비문자열일 경우 경고가 발생합니다.                                                        |
| `deploymentEnvironment`     | `string`                                        | 선택      | 배포 환경을 설정합니다. `setGlobalAttributes()`를 통해 지속되는 `environment` 속성의 값을 설정할 수 있습니다.                                                      |
| `defaultAttributes`         | `Attributes`                                    | 선택      | 기본 리소스 속성을 설정합니다. 모든 Span에 추가되는 전역 속성으로 사용됩니다.                                                                                        |
| `samplingProbability`       | `number \| string`                              | 선택      | 샘플링 확률을 설정합니다. 0에서 1 사이의 값으로 지정하며, 기본값은 `1`입니다.                                                                                         |
| `advancedNetworkCapture`    | `boolean`                                       | 선택      | 네트워크 요청 및 응답의 상세 추적을 활성화합니다. 기본값은 `false`입니다.                                                                                               |
| `blockClass`                | `string`                                        | 선택      | 세션 리플레이에서 가려야 할 요소의 CSS 클래스를 지정합니다.                                                                                                           |
| `consoleCapture`            | `boolean`                                       | 선택      | 콘솔 캡처 기능을 활성화합니다. 기본값은 `false`입니다.                                                                                                                |
| `debug`                     | `boolean`                                       | 선택      | 디버깅 모드를 활성화합니다. 기본값은 `false`입니다.                                                                                                                   |
| `disableIntercom`           | `boolean`                                       | 선택      | Intercom 연동을 비활성화합니다. 기본값은 `false`입니다.                                                                                                               |
| `disableReplay`             | `boolean`                                       | 선택      | 세션 리플레이 기능을 비활성화합니다. 기본값은 `false`입니다.                                                                                                           |
| `ignoreClass`               | `string`                                        | 선택      | 세션 리플레이에서 무시할 요소의 CSS 클래스를 지정합니다.                                                                                                             |
| `ignoreUrls`                | `Array<string \| RegExp>`                       | 선택      | 추적하지 않을 URL 패턴을 지정합니다. 정규식이나 문자열 배열로 설정할 수 있으며, 부분 일치 또는 정확히 일치하는 URL은 추적되지 않습니다.                                 |
| `instrumentations`          | `Instrumentations`                              | 선택      | 사용자 지정 Instrumentations 설정을 지정합니다. 기본값은 빈 객체 `{}`입니다.                                                                                           |
| `maskAllInputs`             | `boolean`                                       | 선택      | 모든 입력 요소를 마스킹합니다. 기본값은 `true`입니다.                                                                                                                 |
| `maskAllText`               | `boolean`                                       | 선택      | 모든 텍스트를 마스킹합니다. 기본값은 `false`입니다.                                                                                                                 |
| `maskClass`                 | `string`                                        | 선택      | 텍스트 마스킹에 사용할 CSS 클래스를 지정합니다.                                                                                                                       |
| `recordCanvas`              | `boolean`                                       | 선택      | 캔버스 요소 기록 여부를 설정합니다. 기본값은 `false`입니다.                                                                                                           |
| `sampling`                  | `RumRecorderConfig['sampling']`                 | 선택      | 세션 레코더 샘플링 설정을 지정합니다.                                                                                                                                    |
| `tracePropagationTargets`   | `Array<string \| RegExp>`                       | 선택      | Trace Header를 전파할 대상 도메인 또는 정규식 목록을 지정합니다.                                                                                                       |

## 옵션 상세 설명

### `collectorUrl` (`string`, 선택)
텔레메트리 데이터를 전송할 콜렉터의 URL을 지정합니다. 기본값은 `https://in-otel.imqa.io`입니다. 필요에 따라 커스터마이즈할 수 있습니다.

### `authorizationToken` (`string`, 선택)
콜렉터에 인증할 때 사용할 토큰을 설정합니다. 보안이 필요한 경우 사용합니다.

### `serviceName` (`string`, 필수)
서비스의 이름을 지정합니다. 이 값은 텔레메트리 데이터 저장 및 분석에 사용됩니다. 필수 옵션으로 설정하지 않으면 텔레메트리 데이터가 저장되지 않습니다.

### `serviceKey` (`string`, 필수)
서비스의 키를 설정합니다. 이 값 역시 텔레메트리 데이터 저장에 필수적입니다.

### `serviceVersion` (`string`, 필수)
서비스의 버전을 지정합니다. 빈 문자열이거나 문자열이 아닌 경우 경고가 발생하며, 텔레메트리 데이터가 저장되지 않습니다.

### `deploymentEnvironment` (`string`, 선택)
배포 환경을 설정합니다. 예를 들어, `production`, `staging` 등으로 설정하여 환경별로 데이터를 구분할 수 있습니다.

### `defaultAttributes` (`Attributes`, 선택)
모든 Span에 추가되는 전역 속성을 설정합니다. 사용자 정의 속성을 추가하여 텔레메트리 데이터를 보강할 수 있습니다.

### `samplingProbability` (`number | string`, 선택)
샘플링 확률을 설정합니다. 0에서 1 사이의 값으로 지정하며, 기본값은 `1`로 모든 트랜잭션을 샘플링합니다.

### `advancedNetworkCapture` (`boolean`, 선택)
네트워크 요청 및 응답의 상세한 추적을 활성화합니다. 기본값은 `false`이며, 활성화 시 네트워크 관련 데이터를 더 상세히 수집할 수 있습니다.

### `blockClass` (`string`, 선택)
세션 리플레이에서 가려야 할 요소의 CSS 클래스를 지정합니다. 민감한 정보가 포함된 요소를 가릴 때 사용합니다.

### `consoleCapture` (`boolean`, 선택)
콘솔 로그를 캡처할지 여부를 설정합니다. 기본값은 `false`이며, 활성화 시 콘솔에서 발생하는 로그도 텔레메트리 데이터로 수집됩니다.

### `debug` (`boolean`, 선택)
디버깅 모드를 활성화합니다. 기본값은 `false`이며, 활성화 시 추가적인 로그가 출력되어 문제 해결에 도움이 됩니다.

### `disableIntercom` (`boolean`, 선택)
Intercom과의 연동을 비활성화합니다. 기본값은 `false`이며, 활성화 시 Intercom과의 통합 기능이 비활성화됩니다.

### `disableReplay` (`boolean`, 선택)
세션 리플레이 기능을 비활성화합니다. 기본값은 `false`이며, 활성화 시 사용자 세션의 리플레이 기능이 동작하지 않습니다.

### `ignoreClass` (`string`, 선택)
세션 리플레이에서 무시할 요소의 CSS 클래스를 지정합니다. 특정 요소를 기록하지 않으려는 경우 사용합니다.

### `ignoreUrls` (`Array<string | RegExp>`, 선택)
추적하지 않을 URL 패턴을 지정합니다. 정규식이나 문자열 배열로 설정할 수 있으며, 부분 일치하거나 정확히 일치하는 URL은 텔레메트리 데이터에 포함되지 않습니다.

### `instrumentations` (`Instrumentations`, 선택)
사용자 지정 Instrumentations 설정을 지정합니다. 기본값은 빈 객체 `{}`이며, 필요에 따라 특정 트래픽 또는 기능만을 추적하도록 설정할 수 있습니다.

`IMQAOtelWebOptionsInstrumentations` 인터페이스는 다양한 계측 모듈의 활성화 여부 및 설정을 정의합니다. 각 속성은 특정 계측 기능을 활성화하거나 비활성화할 수 있으며, 추가적인 설정을 제공할 수 있습니다.

#### 속성 목록

| 속성 이름         | 타입                                               | 필수 여부 | 설명                                                                                                       |
|------------------|----------------------------------------------------|-----------|------------------------------------------------------------------------------------------------------------|
| `console`        | `boolean` 또는 `IMQAConsoleInstrumentation`          | 선택      | 콘솔 계측을 활성화하거나 비활성화합니다. `IMQAConsoleInstrumentation` 객체를 통해 추가 설정이 가능합니다.   |
| `document`       | `boolean` 또는 `InstrumentationConfig`              | 선택      | 문서 로드 계측을 활성화하거나 비활성화합니다. `InstrumentationConfig`을 사용하여 계측 세부 설정을 지정할 수 있습니다. |
| `errors`         | `boolean`                                          | 선택      | 오류 계측을 활성화하거나 비활성화합니다.                                                                  |
| `fetch`          | `boolean` 또는 `IMQAFetchInstrumentationConfig`     | 선택      | Fetch API 계측을 활성화하거나 비활성화합니다. `IMQAFetchInstrumentationConfig`을 통해 추가 설정이 가능합니다. |
| `interactions`   | `boolean` 또는 `IMQAUserInteractionInstrumentationConfig` | 선택      | 사용자 상호작용 계측을 활성화하거나 비활성화합니다. `IMQAUserInteractionInstrumentationConfig`을 통해 세부 설정을 지정할 수 있습니다. |
| `longtask`       | `boolean` 또는 `InstrumentationConfig`              | 선택      | 장시간 작업 계측을 활성화하거나 비활성화합니다. `InstrumentationConfig`을 사용하여 계측 세부 설정을 지정할 수 있습니다. |
| `visibility`     | `boolean` 또는 `InstrumentationConfig`              | 선택      | 페이지 가시성 계측을 활성화하거나 비활성화합니다. `InstrumentationConfig`을 통해 추가 설정이 가능합니다.      |
| `connectivity`   | `boolean` 또는 `InstrumentationConfig`              | 선택      | 네트워크 연결 상태 계측을 활성화하거나 비활성화합니다. `InstrumentationConfig`을 사용하여 계측 세부 설정을 지정할 수 있습니다. |
| `postload`       | `boolean` 또는 `IMQAPostDocLoadResourceInstrumentationConfig` | 선택      | 문서 로드 후 리소스 계측을 활성화하거나 비활성화합니다. `IMQAPostDocLoadResourceInstrumentationConfig`을 통해 추가 설정이 가능합니다. |
| `socketio`       | `boolean` 또는 `SocketIoClientInstrumentationConfig` | 선택      | Socket.io 클라이언트 계측을 활성화하거나 비활성화합니다. `SocketIoClientInstrumentationConfig`을 통해 세부 설정을 지정할 수 있습니다. |
| `websocket`      | `boolean` 또는 `InstrumentationConfig`              | 선택      | WebSocket 계측을 활성화하거나 비활성화합니다. `InstrumentationConfig`을 사용하여 계측 세부 설정을 지정할 수 있습니다. |
| `webvitals`      | `boolean`                                          | 선택      | Web Vitals 계측을 활성화하거나 비활성화합니다.                                                            |
| `xhr`            | `boolean` 또는 `IMQAXMLHttpRequestInstrumentationConfig` | 선택      | XMLHttpRequest 계측을 활성화하거나 비활성화합니다. `IMQAXMLHttpRequestInstrumentationConfig`을 통해 추가 설정이 가능합니다. |

#### 속성 상세 설명

##### `console` (`boolean` 또는 `IMQAConsoleInstrumentation`, 선택)
콘솔 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 기본 콘솔 계측이 활성화됩니다. `IMQAConsoleInstrumentation` 객체를 제공하여 콘솔 계측의 세부 설정을 커스터마이즈할 수 있습니다.

##### `document` (`boolean` 또는 `InstrumentationConfig`, 선택)
문서 로드 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 문서 로드 이벤트가 계측됩니다. `InstrumentationConfig`을 제공하여 계측의 세부 설정을 조정할 수 있습니다.

##### `errors` (`boolean`, 선택)
오류 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 전역 오류 핸들러가 활성화되어 애플리케이션에서 발생하는 오류가 자동으로 계측됩니다.

##### `fetch` (`boolean` 또는 `IMQAFetchInstrumentationConfig`, 선택)
Fetch API 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 Fetch 요청이 자동으로 계측됩니다. `IMQAFetchInstrumentationConfig`을 제공하여 Fetch 계측의 세부 설정을 조정할 수 있습니다.

##### `interactions` (`boolean` 또는 `IMQAUserInteractionInstrumentationConfig`, 선택)
사용자 상호작용 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 클릭, 입력 등 사용자 상호작용 이벤트가 자동으로 계측됩니다. `IMQAUserInteractionInstrumentationConfig`을 통해 상호작용 계측의 세부 설정을 커스터마이즈할 수 있습니다.

##### `longtask` (`boolean` 또는 `InstrumentationConfig`, 선택)
장시간 작업 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 긴 시간 동안 실행되는 작업이 자동으로 계측됩니다. `InstrumentationConfig`을 제공하여 계측의 세부 설정을 조정할 수 있습니다.

##### `visibility` (`boolean` 또는 `InstrumentationConfig`, 선택)
페이지 가시성 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 페이지의 가시성 상태(포그라운드/백그라운드 변화)가 자동으로 계측됩니다. `InstrumentationConfig`을 통해 계측의 세부 설정을 조정할 수 있습니다.

##### `connectivity` (`boolean` 또는 `InstrumentationConfig`, 선택)
네트워크 연결 상태 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 네트워크 상태(온라인/오프라인 변화)가 자동으로 계측됩니다. `InstrumentationConfig`을 제공하여 계측의 세부 설정을 조정할 수 있습니다.

##### `postload` (`boolean` 또는 `IMQAPostDocLoadResourceInstrumentationConfig`, 선택)
문서 로드 후 리소스 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 문서가 완전히 로드된 후 리소스 로딩 이벤트가 자동으로 계측됩니다. `IMQAPostDocLoadResourceInstrumentationConfig`을 통해 계측의 세부 설정을 커스터마이즈할 수 있습니다.

##### `socketio` (`boolean` 또는 `SocketIoClientInstrumentationConfig`, 선택)
Socket.io 클라이언트 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 Socket.io 클라이언트의 이벤트가 자동으로 계측됩니다. `SocketIoClientInstrumentationConfig`을 제공하여 계측의 세부 설정을 조정할 수 있습니다.

##### `websocket` (`boolean` 또는 `InstrumentationConfig`, 선택)
WebSocket 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 WebSocket 연결 이벤트가 자동으로 계측됩니다. `InstrumentationConfig`을 통해 계측의 세부 설정을 조정할 수 있습니다.

##### `webvitals` (`boolean`, 선택)
Web Vitals 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 Core Web Vitals 지표가 자동으로 계측됩니다.

##### `xhr` (`boolean` 또는 `IMQAXMLHttpRequestInstrumentationConfig`, 선택)
XMLHttpRequest 계측을 활성화하거나 비활성화합니다. `true`로 설정하면 XMLHttpRequest 요청이 자동으로 계측됩니다. `IMQAXMLHttpRequestInstrumentationConfig`을 통해 계측의 세부 설정을 조정할 수 있습니다.


## 추적 컨텍스트 전파

기본적으로, 교차 출처 요청에 대한 전체 및 앞 끝에서부터 뒷 부분으로의 추적을 생성하는 추적 컨텍스트 전파는 브라우저 CORS 보안 제한 때문에 활성화되지 않습니다.

여러 출처 서비스의 유효한 구성을 IMQA는 수행할 수 없으므로, 이 설정을 할 때는 주의해 주세요. 프로덕션 설정 전에 테스트 환경에서 CORS를 활성화하는 것이 좋습니다.

예를 들어:

```javascript
// https://api.imqa.io 또는 http://localhost:3000/api URL로 만든 요청에 추적 컨텍스트 전파
propagateTraceHeaderCorsUrls: [
  /^https:\/\/api\.imqa.io\/.*/,
  /^http:\/\/localhost:3000\/api\/.*/,
],
```

### Baggage

Baggage는 스팬 간에 전달되는 컨텍스트 정보입니다. 이는 트레이스의 스팬 컨텍스트와 함께 존재하는 key-value 저장소로, 해당 트레이스 내에서 생성된 모든 스팬에서 값을 사용할 수 있게 합니다.

예를 들어, 여러 서비스에 걸쳐 있는 모든 스팬에 `customerId` 속성을 추가하고 싶다고 가정해 보겠습니다. 하지만 `customerId`는 특정 서비스에서만 제공됩니다. 이 목표를 달성하려면 Baggage를 사용하여 이 값을 시스템 전반에 전파할 수 있습니다. 따라서 Baggage는 동적으로 데이터를 추가할 수 있는 `defaultAttributes`와는 다릅니다. `setDefaultAttribute()`는 유사한 기능을수행할 수 있지만, 이는 후행적인 속성을 추가하는 기능이 없습니다 (한번 전송된 스팬에는 속성을 추가할 수 없습니다).

IMQA는 컨텍스트 전파를 사용하여 Baggage를 전달하며, 각기 다른 라이브러리 구현들은 Baggage를 해석하고 제공하는데 필요한 전파자(propagator)를 가지고 있으므로 별도의 구현이 필요 없습니다. Baggage는 IMQA에서 가져온 메서드를 사용하여 접근할 수 있습니다:

```javascript
const baggage = window.imqa.api.propagation.getBaggage(
  window.imqa.api.context.active(),
) || window.imqa.api.propagation.createBaggage();

baggage.setEntry('customerId', { value: 'customer-id-value' });

window.imqa.api.propagation.setBaggage(
  window.imqa.api.context.active(),
  baggage,
);
```
> [!NOTE]
> [OpenTelemetry Baggage](https://opentelemetry.io/docs/concepts/signals/baggage/) 참조해주세요.

## 수동 계측

`<script />` 태그로 초기화되면, `window` 속성에 `imqa`가 노출됩니다. 이는 수동으로 스팬을 생성할 수 있는 기능을 제공합니다.

글로벌 `imqa` 객체는 다음을 포함합니다:
- `api` - [@opentelemetry/api](https://github.com/open-telemetry/opentelemetry-js/tree/main/api) 모듈 노출
- `tracer` - [@opentelemetry/sdk-trace-base
](https://github.com/open-telemetry/opentelemetry-js/blob/2a4919c1cf99d3403d387d7589836fd9e3018896/packages/opentelemetry-sdk-trace-base/src/Tracer.ts#L36)에서 가져온 `Tracer`의 인스턴스
- `recordError` - 주어진 메시지와 선택적 속성으로 오류를 생성하는 함수

예시:

```javascript
const { tracer, api, recordError } = imqa;

const span = tracer.startSpan('fetchUserData', {
  attributes: {
    organization: 'client-a',
  },
});

api.context.with(api.trace.setSpan(api.context.active(), span), () => {
  // long running operation
});

recordError('Cannot load data', { organization: 'test' });
```

프로덕션 사용 시, `imqa`가 정의되지 않은 경우에도 웹사이트가 정상 작동하는지 확인해야 합니다. (예: 브라우저 확장 프로그램에 의해 차단된 경우).

## 계측 비활성화

계측은 런타임 동안 `registerInstrumentations()` 및 `disableInstrumentations()` 메서드를 사용하여 비활성화하고 다시 활성화할 수 있습니다.

```javascript
imqa.disableInstrumentations();
// 계측이 비활성화된 상태로 일부 코드 실행
imqa.registerInstrumentations();
```

## 공개 API

모든 메서드는 `window.imqa` 객체에서 사용할 수 있습니다.

### setDefaultAttribute(key, value)

초기화 동안 지정된 기본 속성 목록을 확장합니다. 예시: `window.imqa.setDefaultAttribute('user_id', userId)`

### getCurrentSessionId()

현재 `session.id` 속성의 값을 반환합니다. 반환된 값은 시간에 따라 변할 수 있으므로 캐싱하지 않아야 합니다. 예시: `window.imqa.getCurrentSessionId()`

### recordError()

주어진 메시지와 선택적 속성으로 오류를 전송합니다. 예시: `window.imqa.recordError('Cannot load data', { organization: 'test' })`

## 예제 프로그램

아래와 같이 예제 파일을 설정합니다.

[예제 다운로드 ES6](/example.zip)
[예제 다운로드 CommonJS](/example-es5.zip)

```bash
├── index.html
└── js
    └── web-agent-v0.0.1.min.js
```


`index.html`을 열고 아래 코드를 추가합니다. collector 주소가 설정되어 있지 않은 경우 오류가 발생하지만 브라우저 inspection 도구를 통해 network 탭에서 Trace와 Log 데이터를 직접 확인할 수 있습니다.

```html {7, 8-27, 38, 41, 54-56, 60-62} showLineNumbers filename="index.html"
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>IMQA Agent Test Example</title>
    <script src="./js/web-agent-v0.0.1.min.js" type="text/javascript"></script>
    <script>
      window.imqa &&
        window.imqa.initialize({
          collectorUrl: "https://collector.imqa.io",
          serviceName: "tester-service",
          collectErrors: {
            error: true,
            typeError: true,
            syntaxError: true,
            referenceError: true,
          },
          collectLogs: {
            log: true,
            info: true,
            debug: true,
            warn: true,
            error: true,
          }
        });
    </script>
  </head>
  <body>
    <h1>IMQA Agent Test Example</h1>
    <button id="work-btn">Do Work</button>
    <button id="error-btn">Throw Error</button>
    <p>Click the buttons to test IMQA tracing and error reporting.</p>
  </body>
  <script>
    // 수동 계측을 위한 예제 함수: 스팬 생성
    function doWork() {
      const span = imqa.tracer.startSpan("doWork");
      // 수동 계측 종료 시점
      setTimeout(() => {
        span.end();
        console.log("Work is done and span is ended");
      }, 1000);
    }

    // 수동 계측을 위한 예제 함수: 에러 발생
    function throwError() {
      throw new Error("Test Error");
    }

    // Attach event handlers
    window.onload = () => {
      document.getElementById("work-btn").addEventListener("click", () => {
        const span = imqa.tracer.startSpan("buttonClick");
        doWork();
        span.end();
      });

      document.getElementById("error-btn").addEventListener("click", () => {
        const span = imqa.tracer.startSpan("buttonClickError");
        throwError();
        span.end();
      });
    };
  </script>
</html>
```